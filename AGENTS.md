# 화투로 — 에이전트 공통 지침 (Cursor 등)

이 저장소에서 작업하기 전에 다음 두 문서를 반드시 읽을 것:

1. **CLAUDE.md** — 아키텍처(엔진 마커·순수성), 구현 원칙 체크리스트, 디자인 방향(사용자 확정 사항). 여기 규칙은 절대 위반 금지.
2. **HANDOFF.md** — 최근 세션까지의 변경 내역(밸런스 v3), 현재 시뮬레이션 캘리브레이션, 합의된 다음 작업(도파민 연출, 멍석 시스템).

핵심 요약: `index.html` 단일 파일(외부 리소스 금지), `==== ENGINE START/END ====` 마커 사이는 DOM 접근 없는 순수 로직. 엔진 수정 시 `node test/test-game.mjs` 필수 실행. 밸런스는 빡빡하게(하향보다 상향 우선), 네이밍은 고스톱/한국 전통 용어.
